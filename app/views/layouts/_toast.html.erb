<%- if flash.any? %>
	<%- flash.each do |type, message| %>
		<div data-controller="toast" class="flex fixed justify-between right-5 top-25 items-center w-full max-w-xs px-4 py-2 rounded-lg opacity-70 text-slate-950 toast-<%= type %>" role="alert">
			<div class="ms-3 text-sm font-normal"><%= message %></div>
			<a href="#" class="text-2xl" data-action="toast#hide">&#10005;</a>
			</button>
		</div>
	<% end %>
<% end %>
